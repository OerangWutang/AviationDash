global:
  scrape_interval: 15s

scrape_configs:
  - job_name: atlas-argus
    metrics_path: /api/metrics
    authorization:
      type: Bearer
      credentials_file: /run/secrets/atlas-argus-metrics-token
    static_configs:
      - targets:
          - api:8100
